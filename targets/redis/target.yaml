target: redis
image: redis
version: "7"
defaults:
  connections: 64
  warmup: 60s
  duration: 300s
cluster:
  backend: kubernetes
  nodes: 3
  replication_factor: 3
  consistency: [none, wait]
capabilities:
  verify_models: [reconciliation]
  faults: [kill-node, pause-node, network-partition]
