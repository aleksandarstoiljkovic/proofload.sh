# proofload Redis replica (redis2, redis3). Replicates from the primary by
# hostname. Same no-auth / no-persistence / protected-mode-off posture as the
# primary so cross-container reads and Sentinel promotion work without a password.
port 6379
protected-mode no
save ""
appendonly no
replicaof redis1 6379
