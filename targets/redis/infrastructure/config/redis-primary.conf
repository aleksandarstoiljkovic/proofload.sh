# proofload Redis primary (redis1). Ephemeral test cluster: no auth, no
# persistence. protected-mode is disabled so peer containers and sentinels can
# reach it by hostname without a password (proofload's redisdriver connects to
# the first endpoint with no password on DB 0).
port 6379
protected-mode no
save ""
appendonly no
